# Prompt to paste into Codex

Inspect this repository and finish deploying the acidic-CO2RR literature alert bot.

Requirements:
- Preserve the scientific inclusion rule in `AGENTS.md`: CO2 + electroreduction + acidic/proton-conducting environment must all be present.
- Review the OpenAlex request, local relevance filter, DOI deduplication, Telegram HTML escaping, first-run behavior, and GitHub Actions state commit.
- Run the test suite and linting; fix any failures without weakening the relevance criteria.
- Confirm the workflow can run both hourly and manually.
- Do not hard-code or print secrets.
- Update the README only when setup instructions need correction.
- Commit the finished changes on a branch and provide a concise deployment checklist. Do not create fake API keys or Telegram identifiers.
